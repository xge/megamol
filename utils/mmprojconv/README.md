## Usage

```bash
$ python convert.py -h
usage: convert.py [-h] [-v] input

Converts a given xml-based mmprj MegaMol project file to the new lua-based definition.

positional arguments:
  input            .mmprj file to convert

options:
  -h, --help       show this help message and exit
  -v, --verbosity
```
